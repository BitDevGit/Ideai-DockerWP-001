# Flexible Multi-Site Nginx Configuration
# Supports: Normal sites, Subdomain multisite, Subdirectory multisite
# Routes site1.local, site2.local and all their subdomains
# Dashboard at localhost

# Upstream for Site 1
upstream wordpress1 {
    server wordpress1:9000;
}

# Upstream for Site 2
upstream wordpress2 {
    server wordpress2:9000;
}

# Upstream for Site 3
upstream wordpress3 {
    server wordpress3:9000;
}

# Dashboard at localhost (default - takes priority)
server {
    listen 80 default_server;
    server_name localhost;

    root /var/www/dashboard;
    index index.html;

    # Logging
    access_log /var/log/nginx/dashboard-access.log;
    error_log /var/log/nginx/dashboard-error.log warn;

    # Serve dashboard
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Static assets
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }
}

# Site 1 - Main domain and wildcard subdomains
# Handles:
# - No-hosts-required: site1.localhost, *.site1.localhost
# - Optional (requires /etc/hosts): site1.local, *.site1.local
server {
    listen 80;
    server_name site1.localhost *.site1.localhost site1.local *.site1.local;
    root /var/www/site1;
    index index.php index.html index.htm;

    # Logging
    access_log /var/log/nginx/site1-access.log;
    error_log /var/log/nginx/site1-error.log warn;

    # WordPress multisite support (handles both subdomain and subdirectory)
    location / {
        try_files $uri $uri/ /index.php?$args;
    }

    # Shared wp-content (themes/plugins) for static assets
    location ^~ /wp-content/ {
        alias /var/www/shared-wp-content/;
        expires 1h;
        add_header Cache-Control "public";
        access_log off;
    }

    # PHP processing - Site 1
    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass wordpress1;
        fastcgi_index index.php;
        include fastcgi_params;
        # IMPORTANT: PHP runs in the wordpress1 container where the docroot is /var/www/html
        # (nginx uses /var/www/site1 for static file existence checks).
        fastcgi_param SCRIPT_FILENAME /var/www/html$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT /var/www/html;
        fastcgi_param PATH_INFO $fastcgi_path_info;
        fastcgi_param HTTP_PROXY "";
        
        # Multisite support - pass subdomain info
        fastcgi_param HTTP_HOST $host;
        fastcgi_param SERVER_NAME $server_name;
    }

    # Static files caching
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Deny access to hidden files
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    # Deny access to wp-config.php
    location ~ wp-config.php {
        deny all;
        access_log off;
        log_not_found off;
    }

    # WordPress admin area
    location ~ ^/wp-admin/ {
        try_files $uri $uri/ /index.php?$args;
    }

    # WordPress multisite uploads (subdomain multisite)
    location ~* ^/files/(.*)$ {
        try_files $uri /wp-includes/ms-files.php?file=$1;
        access_log off;
        log_not_found off;
    }

    # WordPress multisite uploads (subdirectory multisite)
    location ~ ^(/[^/]+/)?files/(.+) {
        try_files $uri /wp-includes/ms-files.php?file=$2;
        access_log off;
        log_not_found off;
    }
}

# Site 2 - Main domain and wildcard subdomains
# Handles:
# - No-hosts-required: site2.localhost, *.site2.localhost
# - Optional (requires /etc/hosts): site2.local, *.site2.local
server {
    listen 80;
    server_name site2.localhost *.site2.localhost site2.local *.site2.local;
    root /var/www/site2;
    index index.php index.html index.htm;

    # Logging
    access_log /var/log/nginx/site2-access.log;
    error_log /var/log/nginx/site2-error.log warn;

    # WordPress multisite support (handles both subdomain and subdirectory)
    location / {
        try_files $uri $uri/ /index.php?$args;
    }

    # Shared wp-content (themes/plugins) for static assets
    location ^~ /wp-content/ {
        alias /var/www/shared-wp-content/;
        expires 1h;
        add_header Cache-Control "public";
        access_log off;
    }

    # PHP processing - Site 2
    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass wordpress2;
        fastcgi_index index.php;
        include fastcgi_params;
        # IMPORTANT: PHP runs in the wordpress2 container where the docroot is /var/www/html
        # (nginx uses /var/www/site2 for static file existence checks).
        fastcgi_param SCRIPT_FILENAME /var/www/html$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT /var/www/html;
        fastcgi_param PATH_INFO $fastcgi_path_info;
        fastcgi_param HTTP_PROXY "";
        
        # Multisite support - pass subdomain info
        fastcgi_param HTTP_HOST $host;
        fastcgi_param SERVER_NAME $server_name;
    }

    # Static files caching
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Deny access to hidden files
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    # Deny access to wp-config.php
    location ~ wp-config.php {
        deny all;
        access_log off;
        log_not_found off;
    }

    # WordPress admin area
    location ~ ^/wp-admin/ {
        try_files $uri $uri/ /index.php?$args;
    }

    # WordPress multisite uploads (subdomain multisite)
    location ~* ^/files/(.*)$ {
        try_files $uri /wp-includes/ms-files.php?file=$1;
        access_log off;
        log_not_found off;
    }

    # WordPress multisite uploads (subdirectory multisite)
    location ~ ^(/[^/]+/)?files/(.+) {
        try_files $uri /wp-includes/ms-files.php?file=$2;
        access_log off;
        log_not_found off;
    }
}

# Site 3 - Main domain only (intended demo: subdirectory multisite)
# Handles:
# - No-hosts-required: site3.localhost
# - Optional (requires /etc/hosts): site3.local
server {
    listen 80;
    server_name site3.localhost site3.local;
    root /var/www/site3;
    index index.php index.html index.htm;

    # Logging
    access_log /var/log/nginx/site3-access.log;
    error_log /var/log/nginx/site3-error.log warn;

    location / {
        try_files $uri $uri/ /index.php?$args;
    }

    # Shared wp-content (themes/plugins) for static assets
    location ^~ /wp-content/ {
        alias /var/www/shared-wp-content/;
        expires 1h;
        add_header Cache-Control "public";
        access_log off;
    }

    # PHP processing - Site 3
    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass wordpress3;
        fastcgi_index index.php;
        include fastcgi_params;
        # IMPORTANT: PHP runs in the wordpress3 container where the docroot is /var/www/html
        # (nginx uses /var/www/site3 for static file existence checks).
        fastcgi_param SCRIPT_FILENAME /var/www/html$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT /var/www/html;
        fastcgi_param PATH_INFO $fastcgi_path_info;
        fastcgi_param HTTP_PROXY "";

        fastcgi_param HTTP_HOST $host;
        fastcgi_param SERVER_NAME $server_name;
    }

    # WordPress admin area
    location ~ ^/wp-admin/ {
        try_files $uri $uri/ /index.php?$args;
    }

    # Multisite uploads (subdirectory multisite)
    location ~ ^(/[^/]+/)?files/(.+) {
        try_files $uri /wp-includes/ms-files.php?file=$2;
        access_log off;
        log_not_found off;
    }

    # Deny access to hidden files
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    # Deny access to wp-config.php
    location ~ wp-config.php {
        deny all;
        access_log off;
        log_not_found off;
    }
}

